import type {Config} from '../config.js'; export type ServiceProfile={vehicle:string;temperatureCapability:string;baseLocation:string;coverage:string[];availability:string;cargoTypes:string[];certifications:string[];documentation:string[];contactPerson:string;phone:string;videoUrl:string}; export function composeEmail(company:string,email:string,evidence:string[],profile:ServiceProfile,config:Config){if(!email||!evidence.length)throw new Error('Cannot draft without verified contact and evidence');return{to:email,subject:`Transporte refrigerado para ${company}`,body:`Hola, ${company}.\n\nEncontramos esta información pública: ${evidence[0]}\n\nOfrecemos ${profile.vehicle} con capacidad ${profile.temperatureCapability} para ${profile.coverage.join(', ')}. ¿Podemos conversar sobre sus recorridos?\n\nSaludos,\n${profile.contactPerson||config.SENDER_NAME}`}}
