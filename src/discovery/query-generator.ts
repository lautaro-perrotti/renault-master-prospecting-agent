const base=['distribuidores de alimentos con entregas','mayoristas de alimentos reparto','proveedor HORECA entrega','productos congelados entregas','distribuidores de lacteos reparto','frutas y verduras mayorista entrega','importador de alimentos entrega comercios','ecommerce entregas propias','buscamos fleteros vehiculo propio','nuevas zonas de entrega','distribuidores de bebidas entrega'];
export class QueryGenerator{constructor(private readonly location='CABA, GBA, AMBA'){}initial(){return base.map(query=>`${query} ${this.location}`)}fromSignals(signals:string[]){return [...new Set(signals.flatMap(signal=>base.filter(query=>query.includes(signal)||signal.includes(query)).map(query=>`${query} ${this.location}`)))]}}
