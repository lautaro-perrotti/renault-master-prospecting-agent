const base=['distribuciÃ³n mayorista','empresas con reparto','reposiciÃ³n diaria comercios','fleteros vehÃ­culo propio','logÃ­stica urbana','distribuciÃ³n HORECA','transporte refrigerado','productos congelados','alimentos preparados','frutas y verduras mayorista','depÃ³sito logÃ­stico','mercaderÃ­a seca mayorista'];
export class QueryGenerator{constructor(private readonly location='CABA, GBA, AMBA'){} initial(){return base.map(query=>`${query} ${this.location}`)} fromSignals(signals:string[]){return [...new Set(signals.flatMap(signal=>base.filter(query=>query.includes(signal)||signal.includes(query)).map(query=>`${query} ${this.location}`)))]}}
