import {ReplyClass} from '../domain.js'; export function sequenceAction(classification:ReplyClass){if(['INTERESTED','QUESTION','NOT_INTERESTED','OPT_OUT','BOUNCE'].includes(classification))return'STOPPED';return classification==='AUTO_REPLY'?'KEEP_ACTIVE':'NO_DECISION'} export function suppressionFor(classification:ReplyClass,email:string|undefined){return classification==='OPT_OUT'&&email?{email,reason:'OPT_OUT_REPLY'}:undefined}
