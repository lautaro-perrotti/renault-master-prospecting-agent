import {google} from 'googleapis'; import readline from 'node:readline/promises'; import {stdin as input,stdout as output} from 'node:process'; const id=process.env.GOOGLE_CLIENT_ID,secret=process.env.GOOGLE_CLIENT_SECRET;if(!id||!secret)throw new Error('NOT_CONFIGURED: GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET');const auth=new google.auth.OAuth2(id,secret,'http://localhost');const url=auth.generateAuthUrl({access_type:'offline',scope:['https://www.googleapis.com/auth/gmail.modify','https://www.googleapis.com/auth/spreadsheets']});console.log(url);const rl=readline.createInterface({input,output});const code=await rl.question('Code: ');rl.close();const token=await auth.getToken(code);console.log(JSON.stringify(token.tokens,null,2));
