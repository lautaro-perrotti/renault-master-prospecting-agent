import {describe,expect,it} from 'vitest'; import {reconcileRows,SheetsSync} from '../src/integrations/sheets.js';
describe('sheets sync',()=>{it('maps stable internal ids to updates and inserts',()=>{const r=reconcileRows([['internal_id','name'],['a','old']], [['a','new'],['b','new']]);expect(r[0].rowNumber).toBe(2);expect(r[1].rowNumber).toBeUndefined()});it('is idempotent against the adapter contract',async()=>{const calls:any[]=[];const api:any={spreadsheets:{values:{get:async()=>({data:{values:[['internal_id','name'],['a','old']]}}),update:async(x:any)=>calls.push(['update',x]),append:async(x:any)=>calls.push(['append',x])}}};const sync=new SheetsSync({GOOGLE_SHEET_ID:'sheet',GOOGLE_REFRESH_TOKEN:'token'} as any,api);await sync.syncTab('LEADS',[['a','new'],['b','new']]);expect(calls.map(x=>x[0])).toEqual(['update','append'])})});
