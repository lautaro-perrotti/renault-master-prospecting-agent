import {describe,expect,it} from 'vitest'; import {composeEmail} from '../src/outreach/composer.js';
const profile={vehicle:'Renault Master refrigerada',temperatureCapability:'-18 a 4 C',baseLocation:'CABA',coverage:['CABA','GBA'],availability:'configurada',cargoTypes:['alimentos'],certifications:[],documentation:[],contactPerson:'Operaciones',phone:'',videoUrl:''};
describe('email composition',()=>{it('uses persisted evidence and profile',()=>{const m=composeEmail('Empresa','logistica@empresa.com',['Distribución pública en CABA'],profile,{SENDER_NAME:'Sender'} as any);expect(m.body).toContain('Distribución pública en CABA');expect(m.body).toContain('Renault Master refrigerada')});it('rejects missing evidence',()=>{expect(()=>composeEmail('Empresa','a@b.com',[],profile,{} as any)).toThrow()});it('rejects missing recipient',()=>{expect(()=>composeEmail('Empresa','', ['evidence'],profile,{} as any)).toThrow()})});
