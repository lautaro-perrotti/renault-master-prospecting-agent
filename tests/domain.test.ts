import {describe,expect,it} from 'vitest'; import {classifyScore,scoreSignals} from '../src/domain.js'; import {extractContacts} from '../src/enrichment/contacts.js'; import {nextFollowUp,shouldStop} from '../src/outreach/sequences.js';
describe('qualification',()=>{it('uses 100 point breakdown and thresholds',()=>{const s=scoreSignals({fit:40,need:25,geography:15,contact:10,recency:10,confidence:'HIGH'});expect(s.total).toBe(100);expect(classifyScore(s,{AUTO_SEND_THRESHOLD:82,REVIEW_THRESHOLD:60})).toBe('AUTO_ELIGIBLE')});it('classifies review and discard',()=>{expect(classifyScore(scoreSignals({fit:25,need:15,geography:10,contact:5,recency:5,confidence:'MEDIUM'}),{AUTO_SEND_THRESHOLD:82,REVIEW_THRESHOLD:60})).toBe('REVIEW');expect(classifyScore(scoreSignals({fit:5,need:3,geography:0,contact:0,recency:0,confidence:'LOW'}),{AUTO_SEND_THRESHOLD:82,REVIEW_THRESHOLD:60})).toBe('DISCARD')})});
describe('contacts and sequences',()=>{it('extracts public contacts with priority',()=>{const r=extractContacts('Ventas: ventas@empresa.com Info: info@empresa.com Tel +54 11 4444-5555','https://empresa.com/contacto');expect(r[0].email).toBe('ventas@empresa.com');expect(r.some(x=>x.phone)).toBe(true)});it('schedules day 3 and stops on human outcomes',()=>{const d=nextFollowUp(1,new Date('2026-01-01T00:00:00Z'));expect(d.toISOString().startsWith('2026-01-04')).toBe(true);expect(shouldStop('INTERESTED')).toBe(true);expect(shouldStop('AUTO_REPLY')).toBe(false)})});
