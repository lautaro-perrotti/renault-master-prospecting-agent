import {describe,expect,it} from 'vitest'; import {sequenceAction,suppressionFor} from '../src/replies/processing.js'; import {validateResearchResult} from '../src/qualification/ai-research.js';
describe('reply outcomes',()=>{it('stops human outcomes and keeps auto replies active',()=>{expect(sequenceAction('INTERESTED')).toBe('STOPPED');expect(sequenceAction('QUESTION')).toBe('STOPPED');expect(sequenceAction('AUTO_REPLY')).toBe('KEEP_ACTIVE');expect(suppressionFor('OPT_OUT','a@example.com')).toEqual({email:'a@example.com',reason:'OPT_OUT_REPLY'})});it('does not create suppression for unknown outcomes',()=>{expect(suppressionFor('UNKNOWN','a@example.com')).toBeUndefined()})});
describe('AI evidence guard',()=>{it('rejects unknown evidence ids',()=>{const result:any={companyType:'x',relevance:1,summary:'x',detectedSignals:[{evidenceId:'missing',signal:'x'}],reasoningSummary:'x',recommendedAction:'REVIEW',confidence:'HIGH'};expect(()=>validateResearchResult(result,['known'])).toThrow('AI_REFERENCES_UNKNOWN_EVIDENCE')});it('accepts only known evidence references',()=>{const result:any={companyType:'x',relevance:1,summary:'x',detectedSignals:[{evidenceId:'known',signal:'x'}],reasoningSummary:'x',recommendedAction:'REVIEW',confidence:'HIGH'};expect(validateResearchResult(result,['known'])).toEqual(result)})});
